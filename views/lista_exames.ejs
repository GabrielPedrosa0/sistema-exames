<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href='../css/lista_exame.css'>
    <link rel="stylesheet" href='../css/MQlistar_exame.css'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css">

    <title>Exames de <%= paciente %></title>
</head>
<body>
    <div class="topo">
        <button>
            <i class="ri-logout-box-fill"></i>
            <a href="/logout">Desconectar</a>
        </button>

        <h1>Exames de <%= paciente %></h1>
        
    </div>
    
    <table border="1" cellpadding="10" cellspacing="0" style="margin: 0 auto;">
        <tr>
            <th>Exame</th>
            <th>Status</th>
            <th>Resultado</th>
        </tr>
        <% exames.forEach(exame => { %>
        <tr>
            <td><%= exame.nome_exame %></td>
            <td><%= exame.status %></td>
            <td>
                <% if (exame.status === 'pronto' && exame.resultado_url) { %>
                    <a href="<%= exame.resultado_url %>" target="_blank">Baixar</a>
                <% } else { %>
                    Pendente
                <% } %>
            </td>
        </tr>
        <% }) %>
    </table>
</body>
</html>
